/*
 * Placeholder MCU sketch.
 *
 * In this phase the Smart Cane gateway runs entirely on the UNO Q **Linux** side
 * (BLE receive + TimeSeriesStore + WebUI). The STM32 MCU is not used, but App Lab
 * apps keep a sketch/ slot — this no-op keeps the project structure complete.
 */
void setup() {}
void loop() {}
