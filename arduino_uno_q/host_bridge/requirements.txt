# Host-side REST BLE bridge (run on the UNO Q Debian host, NOT in the App Lab container).
#
# Version pins are tested floors (compatible-release). Confirm/refresh against the board with:
#   python3 -m pip freeze | grep -iE 'bleak|fastapi|uvicorn'
bleak~=0.22
fastapi~=0.115
uvicorn~=0.30
